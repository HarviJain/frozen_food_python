# Abhyuday Bharat Food Cluster — Backend Dependencies
# Install: pip install -r requirements.txt

Flask==3.0.3
Flask-Cors==4.0.1
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.3
python-dotenv==1.0.1

# Production server (optional — use instead of Flask dev server)
gunicorn==22.0.0

# PostgreSQL driver (optional — only needed if using PostgreSQL in prod)
# psycopg2-binary==2.9.9